{
  "name": "dex8-skript",
  "version": "1.0.0",
  "description": "A DEX8 skript is a Node.js script that runs on the dex8.com Serverless platform.",
  "main": "main.js",
  "scripts": {
    "inst": "rm -rf node_modules && rm package-lock.json && npm install",
    "inst-win": "rmdir /S /Q node_modules && del package-lock.json && npm install"
  },
  "license": "MIT",
  "dependencies": {
    "@mikosoft/echo": "^1.0.1",
    "@mikosoft/functionflow": "^1.0.1",
    "@mikosoft/httpclient-pptr": "^2.0.0",
    "puppeteer-core": "^5.1.0"
  }
}